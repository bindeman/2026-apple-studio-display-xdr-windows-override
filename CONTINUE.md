# Continue Prompt

Paste this into Codex after reboot:

```text
Continue from C:\Users\Beast\Documents\Dev\Apple Studio Display XDR Windows.
Read README.md, CONTINUE.md, and current git status first.

Current state:
- 2026-09-12 wrap-up session: all previously uncommitted work (WPF control panel, setup launcher, scripts, docs) was committed and pushed, PR #1 was merged into main, and v0.1.0 was tagged. The release workflow now builds two flavors: `studio-display-xdr-windows.zip` (full, GUI) and `studio-display-xdr-windows-scripts.zip` (scripts only, no GUI). Version comes from the repo-root `VERSION` file and must match the `v*` tag.
- GitHub issue #3 (Windows PowerShell 5.1: `Select-Object : Property "PSObject" cannot be found` at the end of Install-StudioXdrEdidOverride.ps1) is fixed: stale-block cleanup now uses RegistryKey.GetValueNames() and is non-fatal. The bug reproduced locally under PS 5.1 before the fix. Guided installer and support report no longer call `dotnet` unguarded.
- GitHub issue #2 (reading automatic-brightness / ambient data) was answered: current brightness is readable through the Apple HID path (`StudioXdrBrightness.ps1 -Status`, `Brightness-Status.cmd`); the native ambient sensor on PID 0x1116 `MI_08` is still not readable on Windows (HidUsb Code 10), Windows exposes no LightSensor for it, and camera fallback is the practical proxy. See docs\auto-brightness.md and docs\hid-research.md.
- `Uninstall-StudioXdrEdidOverride.ps1 -All` removes every project-created EDID override even when the XDR is not active (`-All -Force` also removes CRU-style overrides). docs\recovery.md now covers Safe Mode and WinRE offline registry recovery for the Boot Camp black-screen report in issue #3.
- Local hardware at that session: the Studio Display XDR (PID 0x1116) was NOT connected. A Pro Display XDR (APPAE22, 6016x3384 @ 60 Hz, HDR on) and a 2022 Studio Display (APPAE3A, PID 0x1114, 5120x2880 @ 60 Hz) were connected instead, so the XDR hardware gate could not be re-run. Gates that WERE run and passed: build (both flavors), ZIP verification (both flavors), extracted-package smoke (both flavors). NOT run this session (out of time): app runtime smoke, app UI smoke, and a multi-agent PS 5.1 compatibility review whose results were still pending. Next session: run `scripts\Test-StudioDisplayXdrRelease.ps1` end to end with the XDR connected, check the GitHub Actions run for the v0.1.0 tag, and confirm both ZIPs are attached to the release.
- We got the 2026 Apple Studio Display XDR working in Windows at 5120x2880. 120 Hz is available and has tested successfully; current local state at the last read-only check was 5120x2880 @ 120.042 Hz with HDR / Advanced Color on.
- Native mode works via a Studio Display XDR EDID override.
- HDR / Advanced Color can be toggled through the Windows DisplayConfig API.
- The reusable HDR command is:
  powershell -ExecutionPolicy Bypass -File .\scripts\Enable-StudioXdrHdr.ps1
- The status command is:
  powershell -ExecutionPolicy Bypass -File .\scripts\Get-StudioXdrStatus.ps1
- GitHub repo:
  https://github.com/bindeman/2026-apple-studio-display-xdr-windows-override
- Local origin points to that repo.
- Repo description:
  Enable 5K 120Hz HDR on Apple Studio Display XDR in Windows.

Next goal:
Continue hacking first-party-style Windows support: brightness UI, automatic brightness, HDR/color behavior, gaming tearing diagnostics, packaging, screenshots, docs, or installer hardening.
Current investigation:
- Refresh-rate toggling can clear a transient right-side pixelated/tile artifact.
- Color profile support is partially packaged. Windows gets EDID wide-color/HDR metadata, and the repo now includes `profiles\StudioDisplayXDR-DisplayP3.icm`, a generated Display P3 D65 gamma 2.2 baseline profile. This is not an Apple-authored reference-mode profile.
- Color status now works with scripts\Get-StudioXdrColorStatus.ps1. Latest local result through readiness: Studio XDR on \\.\DISPLAY1, 5120x2880 @ 120.042 Hz, Advanced Color supported and on, 10 bpc.
- Check-Color.cmd runs `scripts\Get-StudioXdrColorStatus.ps1` and `scripts\Find-StudioXdrColorProfiles.ps1 -IncludeUserFolders`. The profile discovery parses ICC headers/descriptions, scans the bundled profile folder, Windows color store, likely Apple install folders, and optional user folders, then flags Apple/Studio/XDR/ProDisplay/P3/Adobe/HDR candidates. Latest local default result found the generated Studio Display XDR Display P3 profile in the repo and Windows color store plus standard Windows profiles, but no Apple-authored Studio/Pro Display profile.
- scripts\Install-StudioXdrColorProfile.ps1 can install and associate an ICC/ICM profile with the active Studio XDR target. `Install-Color-Profile.cmd` installs the generated Display P3 baseline profile. `scripts\New-StudioXdrDisplayP3Profile.ps1` regenerates it.
- Brightness control works through Apple USB HID with scripts\StudioXdrBrightness.ps1 and the compiled WPF app in dist\StudioDisplayXdr.App\StudioDisplayXdr.exe.
- Studio-Display-XDR.cmd opens the clean Studio-only brightness app.
- The app has one brightness slider per detected Studio Display XDR, current signal/HDR status, a one-click Optimize button for 5120x2880 @ 120 Hz plus HDR / Advanced Color, manual 60/120 Hz controls, a manual HDR toggle, automatic brightness with persistent enabled/minimum/maximum/response controls under `%LOCALAPPDATA%\StudioDisplayXdr\settings.json`, and an Open at login toggle that writes a `--minimized` startup entry.
- The WPF app now has notification-area background mode. Startup with `--minimized` hides the app to the tray instead of leaving it minimized in the taskbar. The tray menu includes Open Control Panel, Refresh Displays, and Exit; when Open at login is enabled, minimize/close keeps the app available in the tray.
- The WPF app now enforces a single instance with a per-user mutex and a show-window event. Launching it again signals the existing process to show the control panel instead of creating duplicate tray icons. `scripts\Test-StudioDisplayXdrAppRuntime.ps1` smoke-tests hidden startup, duplicate-launch prevention, and second-launch show behavior.
- `scripts\Test-StudioDisplayXdrAppUi.ps1` launches the packaged app and clicks the Color profile, Automatic brightness, Diagnostics `Check`, and Diagnostics `Report` buttons through UI Automation. It verifies that each button returns an in-app result string, and that the generated support report includes the App Configuration section, instead of relying on manual clicking.
- `scripts\Test-StudioDisplayXdrRelease.ps1` is the one-command local release gate. It runs build, ZIP verification, extracted-package smoke, app runtime smoke, and app UI smoke sequentially. GitHub Actions uses the same script with `-SkipInteractiveApp -Quiet`.
- The app has a Diagnostics card with `Check` and `Report` actions. `Check` runs `scripts\Test-StudioXdrReadiness.ps1 -Json` and summarizes pass/warn/fail counts in-app. `Report` runs `scripts\Get-StudioXdrSupportReport.ps1` and writes a report under `reports\` from the GUI.
- The app has an About row that reads `PACKAGE.json`, displays package build/runtime/source metadata, and has a Copy action for support issues. `Test-StudioDisplayXdrAppUi.ps1` verifies that package metadata is visible.
- `docs\screenshots\control-panel-about.png` captures the lower control-panel section, including Diagnostics, Open at login, and About/build metadata. `scripts\Capture-StudioXdrAppScreenshot.ps1 -Bottom` creates it.
- The guided installer now tests 5120x2880 @ 120 Hz and can apply it only when the user explicitly confirms or passes `-ApplyNativeMode`. `-NoPrompt` does not apply native mode by default.
- The release package now publishes `dist\StudioDisplayXdr.App\StudioDisplayXdr.exe` as a self-contained `win-x64` app, so end users should not need to install the .NET Desktop Runtime separately.
- The app also has a Color profile row. It detects the bundled generated Display P3 profile and exposes a manual install/reinstall button. Do not click it unless the user wants to alter Windows color-profile associations.
- The Color profile row also has a `Check` button that runs `scripts\Find-StudioXdrColorProfiles.ps1 -IncludeUserFolders -Json` from the app. Latest UIAutomation test clicked it successfully and the app reported no Apple-authored Studio/Pro Display profile, with the bundled generated Display P3 baseline available and 4 valid profiles scanned.
- WPF software rendering is enabled in App.xaml.cs so the control panel paints reliably in screenshots/remote capture on the HDR/high-DPI setup.
- `scripts\Capture-StudioXdrAppScreenshot.ps1` captures the packaged control panel to `docs\screenshots\control-panel.png`, which is linked from the README. It now uses `PrintWindow` before falling back to screen capture, so overlapping windows do not corrupt the screenshot.
- The control panel now exposes automatic-brightness Minimum, Maximum, and Response sliders. `AutoMaximumBrightnessPercent` already existed in settings; the UI now exposes it so users can cap camera/native auto-brightness.
- The control panel automatic-brightness row now has a `Check` button that runs `scripts\Test-StudioXdrAmbientPreflight.ps1 -Json` and `scripts\New-StudioXdrAmbientDriverPackage.ps1 -CheckOnly -Json` from the app and summarizes the ambient-source verdict plus catalog/signing blocker in-place. Latest expected local shape: camera fallback ready, native MI_08 not ready, exact target `USB\VID_05AC&PID_1116&REV_1801&MI_08`, WinUSB package matches MI_08 but Windows is still using HidUsb/input.inf, and Prepare Ambient reports WDK signing tools/catalog missing. Direct JSON result also reports Windows ALS false, WinUSB false, libusb false, camera fallback true.
- The control-panel screenshot was refreshed after adding the Maximum slider and ambient `Check` button.
- Automatic brightness now uses best-available source priority: Windows native ALS, experimental Studio XDR MI_08 connector, then Studio Display Camera fallback.
- The Windows native ALS path now enumerates Windows light sensors and prefers Apple/display-looking sensors by name/device ID before falling back to `LightSensor.GetDefault()`. This should allow a connected Pro Display XDR or older Studio Display to act as the ambient source if Windows exposes it through the standard sensor API. The console scripts now use `scripts\WindowsLightSensorTools.ps1` so readiness/ambient checks match the app's enumeration behavior.
- `scripts\Get-AppleDisplaySensorCorrelation.ps1` correlates Apple display USB containers with Windows Sensor-class devices and WinRT `LightSensor` candidates. Latest local result: one Studio Display XDR container, one Studio-owned `MI_09` orientation sensor, no ambient HID sensor, and WinRT LightSensor candidate count 0.
- The Studio Display Camera fallback was tested end-to-end. It captured from `Studio Display Camera`, produced a frame-brightness reading, and adjusted brightness through the Apple HID path.
- Camera fallback now has guardrails: near-black frames pause automatic brightness instead of dimming, and camera readings use a higher minimum brightness floor than real sensor readings. Auto-brightness was turned back off after tests.
- After the early camera test left brightness at 8%, Studio Display XDR brightness was restored to 100% with `scripts\StudioXdrBrightness.ps1 -SetPercent 100`.
- Ambient-light research found USB\VID_05AC&PID_1116&MI_08. Windows currently binds it to input.inf / HidUsb and it fails with Code 10. After the user's driver install attempt, Get-StudioXdrAmbientStatus.ps1 still shows no exact OEM INF in the driver store for USB\VID_05AC&PID_1116&MI_08 and the WinUSB probe still cannot see the interface.
- Get-StudioXdrAmbientStatus.ps1 now reports whether generic libwdi/Zadig packages are installed but not actually bound to MI_08, and prints the exact libusb filter target. Current local result after the user's driver install attempt: target hardware ID is `USB\VID_05AC&PID_1116&REV_1801&MI_08`; `C:\Users\Beast\usb_driver\amd64\install-filter.exe` exists; libwdi generic WinUSB/libusb packages exist in the driver store; MI_08 is still HidUsb/input.inf/Code 10 with no UpperFilters/LowerFilters.
- The app and Test-StudioXdrAmbientConnector.ps1 now detect both the repo's custom WinUSB device-interface GUID and a generic USB device-interface path from a manual Zadig/libwdi binding, as long as the exact MI_08 interface is the device that was bound.
- The app now also includes an optional SDBC-style LibUsbDotNet reader for PID 0x1116, USB configuration 1, interface 8, endpoint 0x89, 18-byte packets, bytes 2..5. This is for a libusb-win32-compatible MI_08 filter path. It builds, but it has not produced a real sensor reading because MI_08 currently has no exact libusb filter on this machine.
- The WinUSB INF now declares `CatalogFile = StudioXdrAmbientWinUsb.cat`, follows Microsoft's documented WinUSB include/needs pattern, and is checked by `Test-StudioXdrAmbientDriverTarget.ps1`. Current local target check: `InfUsesDocumentedWinUsbPattern=True`, `CatalogFileExists=False`, Windows still matches/ranks only `input.inf`.
- `Prepare-Ambient-Driver.cmd` and `scripts\New-StudioXdrAmbientDriverPackage.ps1` are the guarded local test-signing path. They check for WDK `inf2cat.exe` and `signtool.exe`, generate `drivers\StudioXdrAmbientWinUsb.cat`, create/reuse a current-user code-signing cert, and can trust it locally with `-InstallCertificate` from elevated PowerShell. Current local check: WDK tools missing, no catalog, no cert.
- Support reports now include an "Ambient Driver Package Signing" section. The public ZIP intentionally should not bundle a local test-signed `drivers\StudioXdrAmbientWinUsb.cat`.
- scripts\Test-StudioXdrLibUsbAmbient.ps1 is the direct libusb probe. Current local result: it loads LibUsbDotNet from the local NuGet cache and reports no Studio Display XDR libusb device opened because no exact MI_08 filter is installed.
- docs\auto-brightness.md now documents the next libusb-win32 Filter Wizard experiment: select only `vid:05ac pid:1116 rev:1801 mi:08`, reboot, then rerun Get-StudioXdrAmbientStatus.ps1.
- Install-LibUsb-Ambient-Filter.cmd launches scripts\Install-StudioXdrLibUsbFilter.ps1 elevated. It finds C:\Users\Beast\usb_driver\amd64\install-filter.exe if present, targets only the exact MI_08 hardware ID, and asks for confirmation. Do not run it without explicit user approval.
- Install-StudioXdrAmbientConnector.ps1 now reports ConnectorActive and warns clearly when Windows leaves MI_08 on HidUsb instead of pretending the connector is active.
- The guided installer now includes an "Experimental automatic brightness sensor" section. It runs Get-StudioXdrAmbientStatus.ps1 and offers both MI_08 WinUSB and libusb-win32 filter experiments, both defaulting to no. `-NoPrompt` skips both because their defaults are false.
- The guided uninstaller now includes optional cleanup for the experimental libusb-win32 MI_08 filter, defaulting to no. Standalone cleanup launcher: Uninstall-LibUsb-Ambient-Filter.cmd, backed by scripts\Uninstall-StudioXdrLibUsbFilter.ps1. Non-admin smoke test refused to run before touching anything.
- StudioDisplayXdrSetup.exe is now generated at the release ZIP root from src\StudioDisplayXdr.Setup\StudioDisplayXdrSetup.cs using the Windows .NET Framework csc.exe. It is a small WinForms launcher, not a duplicate self-contained .NET runtime. It offers Install/Repair, Open Control Panel, Check Status, Check Ambient, Check Color, Generate Support Report, Uninstall, and Open README. It has version metadata 0.1.0.0, sets DPI awareness, and has a cleaner high-DPI layout after screenshot passes found clipping.
- The package now includes `assets\StudioDisplayXdr.ico`; the WPF control panel uses it as its window/application icon, and the setup launcher embeds it through the .NET Framework compiler.
- The setup launcher now uses a two-column custom action-card grid with concise task descriptions. The latest screenshot pass showed no clipped labels at the current high-DPI capture size.
- scripts\Capture-StudioXdrSetupScreenshot.ps1 captures the setup launcher to docs\screenshots\setup-launcher.png. README.md now shows both the setup launcher and the control panel screenshots. Test-StudioDisplayXdrPackage.ps1 requires both screenshot files in the ZIP.
- Build-StudioDisplayXdrPackage.ps1 stages only `tools\LibUsbDotNet.LibUsbDotNet.net45.dll` into the ZIP from `dist\PackageTools\tools`; it intentionally does not package the ignored local research folder under repo-root `tools`.
- Test-StudioDisplayXdrPackage.ps1 now verifies the root setup EXE MZ header, PACKAGE.json metadata, absence of forbidden research/build folders, and a strict tools-folder allowlist. It strips UTF-8 BOM before parsing PACKAGE.json.
- scripts\Get-AppleDisplayUsbTopology.ps1 is a read-only topology dump for Studio Display XDR, older Studio Display, and Pro Display XDR USB interfaces. It reports MI numbers, services, driver INFs, problem codes, hardware IDs, compatible IDs, container IDs, and location paths. Support reports now include it.
- scripts\Dump-StudioXdrHidCollections.ps1 is a read-only HID collection dump. Current local interpretation is documented in docs\hid-research.md: `MI_07` collection 1 is the working brightness HID path, `MI_08` is the failing native sensor experiment, and `MI_09` is a Sensor Page device-orientation collection, not ambient light.
- StudioBrightness++ is the closest upstream Windows clue. It uses Windows ISensorEvents + ContainerId correlation for ALS, and currently lists PID 0x1116 as no ALS through the Windows Sensor API while PID 0x1118 has built-in ALS.
- Older 2022 Studio Display SDBC code read 18-byte packets from MI_08 endpoint 0x89 and decoded bytes 2..5 as a 0..1,000,000 ambient value. This repo's MI_08 WinUSB path is still experimental and may be a dead end for PID 0x1116.
- New ambient files: drivers\StudioXdrAmbientWinUsb.inf, Install-Ambient-Connector.cmd, scripts\Get-StudioXdrAmbientStatus.ps1, scripts\Test-StudioXdrAmbientConnector.ps1, scripts\Test-StudioXdrAmbientDriverTarget.ps1, docs\auto-brightness.md. The WinUSB INF now includes both `USB\VID_05AC&PID_1116&REV_1801&MI_08` and `USB\VID_05AC&PID_1116&MI_08`, and the package verifier enforces those target IDs. `Test-StudioXdrAmbientDriverTarget.ps1` is a dry-run check for INF target match, driver-store presence, and active binding.
- Camera, microphone, and speakers already enumerate as standard Windows devices.
- HDR works through Windows Advanced Color, but do not overclaim full Apple-native reference-mode support.
- Gaming tearing notes are in docs\gaming-tearing.md. scripts\Set-StudioXdrDisplayMode.ps1 can test/apply 5120x2880 at 60 or 120 Hz through ChangeDisplaySettingsEx. Latest `-RefreshRate 120 -TestOnly` result succeeded.
- Check-Gaming.cmd runs `scripts\Get-StudioXdrGamingStatus.ps1`, a read-only gaming/tearing diagnostic for active Studio XDR signal, HDR/Advanced Color state, 5K120 test, GPU drivers, `nvidia-smi` output when present, HAGS/DirectX/GameConfig registry hints, and first-pass V-Sync/VRR frame-cap recommendations. Latest local result: 5120x2880 @ 120.042 Hz, HDR off / 8 bpc, 5K120 test success, RTX 5090 driver 591.59, and recommended initial cap 118 FPS.
- Support-Report.cmd writes a full diagnostic report under reports\.
- Check-Status.cmd runs `scripts\Test-StudioXdrReadiness.ps1`, a concise read-only doctor summary for display target, native resolution, refresh, HDR/Advanced Color, 5K120 test, brightness HID, camera/audio, Windows ALS, MI_08 native ambient state, and color profile. Latest local result: display target pass, 5120x2880 pass, refresh pass at 120.042 Hz, 5K120 test pass, HDR on at 10 bpc, brightness HID pass at 70%, camera/audio pass, Windows ALS info candidate count 0, MI_08 warn `HidUsb/input.inf` with no filters, generated Display P3 profile installed.
- Check-Ambient.cmd runs `scripts\Test-StudioXdrAmbientPreflight.ps1`, a focused read-only automatic-brightness verdict. The script also supports `-Json` for the WPF app. Latest local result: Windows ALS unavailable, MI_08 present but still `HidUsb` / `input.inf` / Code 10, exact target `USB\VID_05AC&PID_1116&REV_1801&MI_08`, no MI_08 libusb filters, WinUSB/libusb packet probes not ready, Studio Display Camera fallback ready.
- Check-Status.cmd, Check-Ambient.cmd, Check-Color.cmd, and Support-Report.cmd now pause before closing so double-click/launcher users can read the console output. The extracted-package smoke test asserts those console launchers contain `pause`.
- `scripts\Test-StudioXdrReadiness.ps1` now ends with a concise count summary and next steps for warnings/failures, and supports `-Json` for the WPF app. Latest local JSON result: 7 pass, 2 warning, 0 fail, 2 info; result `usable with warnings`.
- Support reports now include a "Readiness Summary" section before the deeper display/USB diagnostics.
- Support reports now include an "App Configuration" section with `%LOCALAPPDATA%\StudioDisplayXdr\settings.json` values, Open at login Run key state, stable `%LOCALAPPDATA%\StudioDisplayXdr\Support` install state, and running `StudioDisplayXdr` process state. The extracted-package smoke test asserts those fields exist.
- StudioDisplayXdrSetup.exe now has `Check Status`, `Check Ambient`, and `Check Color` buttons, wired to `Check-Status.cmd` / `scripts\Test-StudioXdrReadiness.ps1`, `Check-Ambient.cmd` / `scripts\Test-StudioXdrAmbientPreflight.ps1`, and `Check-Color.cmd` / `scripts\Find-StudioXdrColorProfiles.ps1`. The setup screenshot was refreshed and verified visually.
- The setup launcher window was enlarged so the eight-action layout does not clip at high DPI. `docs\screenshots\setup-launcher.png` was refreshed and visually checked after this layout polish.
- The guided installer now creates Start Menu shortcuts named `Studio Display XDR Check Status.lnk`, `Studio Display XDR Check Ambient.lnk`, and `Studio Display XDR Check Color.lnk` when those launchers are present. The guided uninstaller removes those shortcuts.
- Support report Package Artifacts now works from both the development checkout and an extracted release ZIP. This was smoke-tested by extracting dist\studio-display-xdr-windows.zip to `%TEMP%\StudioXdrPackageSmoke-*` and running `scripts\Get-StudioXdrSupportReport.ps1` from inside the extracted package; the report listed root `StudioDisplayXdrSetup.exe`, `StudioDisplayXdr.App\StudioDisplayXdr.exe`, `tools\LibUsbDotNet.LibUsbDotNet.net45.dll`, and `PACKAGE.json`.
- scripts\Test-StudioDisplayXdrExtractedPackage.ps1 automates that extracted-ZIP smoke test. It validates root setup/app/support-report artifacts, checks EXE MZ headers and PACKAGE.json metadata, runs the support report from the extracted folder, and asserts that the report sees root package artifacts. The GitHub Actions release workflow now runs this smoke test after the ZIP verifier.
- The root launchers `Studio-Display-XDR.cmd` and `Apple-Brightness-Tray.cmd` now prefer the extracted release path `StudioDisplayXdr.App\StudioDisplayXdr.exe` and fall back to the dev checkout path `dist\StudioDisplayXdr.App\StudioDisplayXdr.exe`.
- `scripts\Install-StudioDisplayXdrSupport.ps1` now uses the same release-root/dev-dist app discovery for runtime checks, shortcut targets, and final launch. This fixes installed shortcuts from an extracted ZIP pointing at a nonexistent `dist\` path.
- The guided installer now offers to stage the packaged app files into `%LOCALAPPDATA%\StudioDisplayXdr\Support` and points Start Menu/Desktop shortcuts there, so shortcuts survive after the extracted ZIP folder is moved or deleted. This copy is allowlisted: packaged root launchers/docs/scripts/drivers/edid/profiles, `StudioDisplayXdr.App`, `StudioDisplayXdrSetup.exe`, and only `tools\LibUsbDotNet.LibUsbDotNet.net45.dll`.
- From a non-git release package, `Studio-Display-XDR.cmd`, `Apple-Brightness-Tray.cmd`, and the setup launcher's Open Control Panel action now prefer `%LOCALAPPDATA%\StudioDisplayXdr\Support\StudioDisplayXdr.App\StudioDisplayXdr.exe` if it exists. Development checkouts still prefer the local repo build.
- The guided uninstaller now removes the stable `%LOCALAPPDATA%\StudioDisplayXdr\Support` app copy by default, unless `-KeepInstalledApp` is used.
- `scripts\Test-StudioDisplayXdrExtractedPackage.ps1` asserts that the release-root launchers prefer `StudioDisplayXdr.App\StudioDisplayXdr.exe`, so the extracted package launcher path cannot regress silently.
- GitHub issue templates now exist under `.github\ISSUE_TEMPLATE\`: `display_or_install.yml` for normal native-mode/HDR/brightness/install issues, `ambient_sensor.yml` for MI_08/WinUSB/libusb/auto-brightness experiments, and `config.yml` to disable blank issues and point users to `docs\support-matrix.md`.
- `.github\workflows\release.yml` now creates a GitHub Release with `dist\studio-display-xdr-windows.zip` attached when a `v*` tag is pushed, using `RELEASE_NOTES.md` as the release body. It still uploads the ZIP as a workflow artifact.
- Uninstall-StudioDisplayXdrSupport.cmd now launches the guided support-package uninstaller. It removes startup and shortcuts by default, and can optionally remove the generated Display P3 profile association/file and the Studio Display XDR EDID override.
- RELEASE_NOTES.md summarizes the postable package. scripts\Test-StudioDisplayXdrPackage.ps1 opens the ZIP and verifies required files, app runtime DLLs, EDID header, and ICC `acsp` signature. The build script and GitHub Actions workflow run this verifier.
- Build now writes generated PACKAGE.json into the release ZIP. It is ignored by git locally. Support reports include Package Metadata and use a Source State section that does not fail when the package is a downloaded ZIP without `.git`.
- docs\support-matrix.md now tracks the public support status for native resolution, 120 Hz, HDR, brightness, auto brightness, color/profile behavior, camera/audio, Adaptive Sync, and known gaps. The package verifier requires this file.
- docs\parity-audit.md maps the original first-party-support goal to concrete artifacts and explicitly labels remaining gaps: native MI_08 packets, Apple reference modes, Apple-authored color profiles, adapter hardware limits, and unsigned packaging. The package verifier requires this file.
- docs\release-checklist.md now tracks build, extracted-package smoke, hardware, app, documentation, and release gates. The package verifier requires this file.
- Docs now include primary-source notes from Apple's 2026 Studio Display XDR technology overview: non-macOS hosts use EDID/DisplayID, non-macOS color is P3-limited even when BT.2020 capability is reported, the display has front/rear ambient light sensors, and Adaptive Sync/frame scheduling are Apple-documented panel features. The docs avoid claiming full Apple reference-mode parity on Windows.
- Latest local release-gate work added `Test-StudioDisplayXdrRelease.ps1`; run it before tagging a public release. It intentionally runs app runtime/UI checks sequentially because those scripts manage the same `StudioDisplayXdr` process.
- Latest read-only local status after the user's driver install attempt: Studio XDR active at 5120x2880 @ 120.042 Hz, Advanced Color/HDR supported and on, 10 bpc, brightness HID at 70%, camera/audio OK, generated Display P3 profile installed, Windows LightSensor candidate count 0, MI_08 still `HidUsb` / `input.inf` / Code 10 with no exact OEM INF and no UpperFilters/LowerFilters. The generic libwdi packages exist, but MI_08 is still not bound to WinUSB/libusb.
- The control panel was relaunched from `dist\StudioDisplayXdr.App\StudioDisplayXdr.exe` after the rebuild.
- DP 1.4 to USB-C/Thunderbolt adapter path gave black screen; docs note this is likely hardware/link/adapter direction and not fixable unless Windows enumerates a DISPLAY device.
```

## Quick Verification

```powershell
cd "C:\Users\Beast\Documents\Dev\Apple Studio Display XDR Windows"
powershell -ExecutionPolicy Bypass -File .\scripts\Get-StudioXdrStatus.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\Enable-StudioXdrHdr.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\Get-StudioXdrAmbientStatus.ps1
git status
```
